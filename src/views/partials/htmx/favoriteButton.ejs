<!-- Updated favorite button after toggle -->
<button
	hx-patch="/api/animes/<%= anime.id %>/favorite/htmx"
	hx-target="this"
	hx-swap="outerHTML"
	class="bg-<%= isFavorite ? 'red' : 'gray' %>-600 hover:bg-<%= isFavorite ? 'red' : 'gray' %>-700 text-white py-3 px-6 rounded-lg font-semibold transition flex items-center justify-center space-x-2"
	title="<%= isFavorite ? 'Remover dos' : 'Adicionar aos' %> favoritos"
>
	<span><%= isFavorite ? '❤️' : '🤍' %></span>
</button>
